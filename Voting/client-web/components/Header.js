import Head from "next/head";
import React, { useEffect } from "react";

const Header = () => {
  return (
    <Head>
      <title>Orbs Voting</title>
      <meta name="viewport" content="initial-scale=1.0, width=device-width" />
    </Head>
  );
};

export default Header;
